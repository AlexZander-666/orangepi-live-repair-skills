interface:
  display_name: "Serial Shell Recovery"
  short_description: "Recover polluted serial shell sessions"
  default_prompt: "Use $serial-shell-debug-recovery to recover a polluted serial shell session and clean serial command output before further debugging."
